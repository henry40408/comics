data
!data/.keep
target

# CI passes the version in as GIT_VERSION; excluding .git stops every commit
# from invalidating the `COPY . .` layer and the build behind it.
.git
.github

# Browser tests: a separate workspace whose target/ the rule above misses.
e2e
